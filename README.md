# dsh-deepseek-billing

> DeepSeek 峰谷计费插件（梁文峰 / 梁文谷）：实时判定当前处于官方高峰时段（高峰翻倍）还是闲时段（半价），并按官方价表（2026-09-03，人民币计价）计算当前会话窗口（含子代理）的 token 消耗与费用，挂载到 ui-beautify 的插件面板。

## ✨ 功能特性

- **会话跟随**：面板跟随当前查看的会话（不可见会话探测组件），切换会话自动切换计费窗口；
- **手动刷新**：状态卡右侧 ↻ 按钮即时刷新；
- **峰谷状态卡**：绿色「梁文谷 · 闲时」/ 红色「梁文峰 · 高峰」徽章（DSH 主题语义色），距下一换档秒级倒计时，周末（周六+周日）全天谷标识；
- **费用计费**：逐事件按官方价表计价（输入未命中 / 缓存命中 / 输出含推理），人民币默认、可切换美元（汇率在线刷新 + 手动 + 默认 7.16）；
- **当前窗口**：当前会话 + 其子代理会话树（含已结束子代理，持久化日志聚合），Token 明细与总消耗；
- **手动价格覆盖**：官方改价时在面板「价格配置」里改即可（localStorage 持久化，可一键恢复官方内置价表）；
- **降级可用**：未安装 ui-beautify 时 console 提示等待 dock 服务，不影响其余插件。

## 安装

```bash
# 开发态（本地源码目录，每次改码后重新安装同步副本）：
dsh plugin --profile web add file:D:/DeepseekPlugin/dsh-deepseek-billing
# 发布态（推荐，钉死提交）：
dsh plugin --profile web add github:Zalpha263/dsh-deepseek-billing#<完整40位commit>
```

> 注意：`file:` 安装得到的是**副本**——每次修改源码后需重新执行上面的 add/install 命令同步，并进行哈希比对；完成后 **Client 改动硬刷新（Ctrl+F5）、Host 改动重启 DSH**。

装完**重启 DSH**（Host 半区加载），在「插件面板」中选择「💰 峰谷计费」页签打开（需同时安装 dsh-ui-beautify 提供插件面板；本插件不占用会话头部位置）。

## 使用说明

| 组件 | 控件 | 作用 |
|------|------|------|
| 状态卡 | 徽章 | 当前时段（梁文峰/梁文谷），峰红谷绿 |
| 状态卡 | 倒计时 | 距下一换档 HH:MM:SS |
| 费用卡 | 总额 | 本次窗口费用（人民币/美元切换） |
| 明细卡 | Token 表 | 缓存命中 / 未命中 / 输出(含推理) / 推理 / 总消耗 |
| 价格配置 | 表单 | 三个模型 × 三项闲时价，高峰自动 ×2，可恢复官方 |

## 计费规则（内置，2026-09-03）

- 高峰（北京）`09:00–12:00`、`14:00–18:00`，其余闲时；闲时价 = 高峰价 × 0.5；
- 峰谷定价 2026-08-17 00:00（北京）起；周末（周六+周日）全天闲时价自 2026-08-23 00:00（北京）起；
- 官方价表（元 / 每 1M tokens，2026-09-03 官方页）：

| 模型 | 缓存命中 闲/峰 | 未命中 闲/峰 | 输出 闲/峰 |
|---|---|---|---|
| deepseek-v4-flash | 0.05 / 0.10 | 1.5 / 3.0 | 4.5 / 9.0 |
| deepseek-v4-pro | 0.15 / 0.30 | 4.5 / 9.0 | 13.5 / 27.0 |
| deepseek-v4-flash-vision-exp | 0.05 / 0.10 | 1.5 / 3.0 | 4.5 / 9.0 |

- 推理 tokens 按输出价（outputTokens 全额）计费；官方价表无独立缓存写入项；
- 2026-08-17 之前的事件无峰谷价可比，计 0 并计入「未计价」提示；非 DeepSeek 模型调用不计价。

## 开发者

- `lib/index.js`：Host 半区（`deepseekBilling` Remote 服务：summary / status / setPrices / setRate），引擎与 `dsh-deepseek-billing` 动态预览版同源；
- `lib/client.js`：Client 半区（手写 `__ModuleLoader__.load` 格式，**纯 DOM** 构建 + dock 面板挂载 + localStorage 持久化；v0.1.6 起不再使用 React root 写宿主容器）；
- 价格/规则常量在 `lib/index.js` 顶部，改完重启 DSH（Host 变更）或硬刷新（Client 变更）即可生效。

## 版本历史（最新在前）

- **v0.1.19**：修复历史/冷会话计费——① **增量折叠自愈**：`scanSession` 由"全量扫一次即永久栅栏"改为按游标增量、每次 poll/刷新核对到日志尾部（折叠对 (turn,step) 幂等，故与事件监听重叠安全），根除"只计开头、刷新没用、监听遗漏后永不再计"类问题；② **模型归因加固**：实测 `request/context` 每会话仅 1 条，现以最近一条 `request/header` 的 `header.config.model` 优先归因（`request/context` 兜底），多模型或压缩后缺上下文事件的会话也能正确计价；③ **会话身份显式化**：客户端恒传当前 `sessionId`（未打开会话为 null），主机不再静默聚合"最近活动会话"、返回 `empty` 占位，面板显示"等待会话…"——杜绝打开历史会话时面板聚合到别的会话；④ 新增 `summary(debug)` 诊断字段（rootOrigin/事件计数/归因模型/错误轨迹，控制台输出）。子代理计数、峰谷判定、别名通道、手动覆盖、汇率全部保留；用真实日志回归（1265 计价步 / 447.6M 命中 tokens，含"隐藏 request/context"与"增量+重扫"两组用例）通过。
- **v0.1.18**：适配 DSH 0.1.2-rc.1 + 别名通道 + 审计加固——① **崩溃级修复**：v0.1.18 早期版本的 `offRow` 在模型无手动覆盖（常态）时对 `undefined` 访问 `ov[bucket]` 抛 TypeError，导致所有官方价计价路径崩溃、面板「加载失败」（已加 `ov === undefined` 守卫与五组回归用例）；② **别名通道**：ECNU 代理模型（`ecnu-max`/`ecnu-image`）按对应 DeepSeek 官方价计价，模型明细标注「经别名通道」；③ 会话事件读取改为 `session.snapshotEvents()`（0.1.2 的按需读取 API，旧 `session.events` 恒 undefined 导致历史会话账目为 0），无冷会话时回退持久化日志；④ 非 DeepSeek 模型明细（按模型名×次数）与「已按对应模型计价」提示；⑤ 删除死代码（`periodCny` 无人消费且语义错误、不可达的「防御补扫」分支、`cursor` 死状态）；`defaultRootId` 两段重复循环合并；⑥ 客户端硬化：token 格式化防 `undefined`、币种 localStorage 脏值校验、汇率回退 7.16（原 `||0` 会除零）；⑦ `dsh.client.inject` 幽灵条目清理、peer 升至 `^0.1.2-rc.1`。遗留：价格配置表单打开时显示官方默认价而非生效覆盖值（既有行为）；`remote.status()` 为保留的服务契约面。

- **v0.1.17**：费用卡增加「本地预估价 · 实际以 API 平台账单为准」提示（悬浮可见完整口径说明：不含 Web 搜索/标题生成等平台侧调用）；
- **v0.1.16**：修复历史会话（冷根）窗口为空/缺根的问题——根会话不在内存（已从内存卸载的旧会话）时回退到持久化日志折叠（与子代理同路径），切到旧会话能正确显示整窗费用（此前只显示子代理或 ¥0）；
- **v0.1.15**：费用卡增加零金额原因说明（调价前/非 DeepSeek 模型调用提示）；
- **v0.1.14**：刷新按钮移入「Token 明细」标题右侧（状态卡不再拥挤）；
- **v0.1.13**：会话跟随修复（不可见会话探测组件随当前会话切换）+ 状态卡手动刷新按钮（↻）；
- **v0.1.12**：插件面板「···（全部插件/搜索）」按钮移至芯片行最前（注入 CSS `order:-1`，不动 ui-beautify）；
- **v0.1.11**：Token 明细增加「总消耗」行（缓存命中+未命中+输出，粗体）；
- **v0.1.10**：价格配置表单重构——分组标题 13pt、行标签 11pt 不换行、删除每行冗长提示、底部统一脚注（单位与 ×2 规则），消除换行与大字号畸形；
- **v0.1.9**：子代理计费改为**持久化日志聚合**（已结束的子代理计入窗口，页脚显示数量）；状态卡间距、费用卡仅保留总额、按钮字号修正（UI 三处调整）；
- **v0.1.8**：UI 按 Apple HIG 打磨——卡片/价格配置改为与 file-explorer 同款毛玻璃表层（替代白色实底）、间距/圆角/字号/行高按 HIG 校准；
- **v0.1.7**：费用卡精简——模型行仅「模型名+金额」（删除 输/出/峰谷 元信息）、删除「本时段已用」、模型行分隔线移除；
- **v0.1.6**：面板内容改为**纯 DOM 构建**（与 file-explorer 同模式）——React root 写入宿主管理容器与宿主同步清空存在异步提交竞争（`removeChild NotFoundError` 打断应用渲染），纯 DOM 消除该竞争（多标签切换修复的核心）；
- **v0.1.5**：`flushSync` 同步提交渲染（中间修复，未解决，被 v0.1.6 取代）；
- **v0.1.4**：移除调试贴片；`mountPanel` 改为 root 复用 + 挂载/卸载日志（排查多标签切换）；
- **v0.1.3**：修复 dock 面板「注册后立即被注销」——清理 effect 内联执行了 disposer，改为返回清理函数（芯片因此不显示）；
- **v0.1.2**：排查期诊断——新增页面内调试贴片（左下角胶囊显示 apply/dock/注册状态）；
- **v0.1.1**：仅挂载插件面板（移除会话头部 💰 入口）；修复客户端 `inject` 缺少 `"remote"` 导致插件不激活的问题；remote 挂载失败降级为只显示「加载失败」；
- **v0.1.0**：初始发布——峰谷判定 + 官方价表计价 + dock 面板 + 价格配置。

## License

MIT
